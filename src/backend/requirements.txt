# ── Runtime ────────────────────────────────────────────────────────────────────
fastapi>=0.111.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.9
pydantic>=2.0.0

# ── Model download ─────────────────────────────────────────────────────────────
huggingface_hub>=0.23.0
requests>=2.31.0  # vocab download in predictor.py + scripts/fetch_wikidata.py

# ── Inference ──────────────────────────────────────────────────────────────────
open-clip-torch>=2.26.1
Pillow>=10.0.0
numpy>=1.26.0

# PyTorch: install separately with the right backend for your platform.
# CUDA (Linux/Windows — recommended if you have an NVIDIA GPU):
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu121
# CPU-only (smaller download, ~2× slower inference):
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cpu
# macOS (Apple Silicon MPS acceleration included automatically):
#   pip install torch torchvision

# ── Dev / test ────────────────────────────────────────────────────────────────
# pytest>=8.0.0
# mypy>=1.10.0
# ruff>=0.4.0
# black>=24.0.0
